# Contracts

Authoritative agent/sync contracts are in the **`vendor/core`** git submodule (Option A).

Read paths:

- `../vendor/core/docs/agent/SYNC_STRATEGY.md`
- `../vendor/core/docs/agent/CORE_DATA_MODEL.md`
- `../vendor/core/docs/agent/WIDGET_AGENT_METADATA_SEED.json`
- `../vendor/core/supabase/migrations/`

This directory is reserved for generated Rust types or CI-synced snapshots if we add code-gen later. Do not hand-edit copies here while the submodule is the source of truth.

Current pin: see [`../CONTRACTS_VERSION`](../CONTRACTS_VERSION).
