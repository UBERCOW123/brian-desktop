# Copy to .env — same Supabase project as CORE mobile (see vendor/core/supabase.local.json.example)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-or-publishable-key

# Desktop OAuth redirect (register in Supabase Auth when enabled)
# OAUTH_REDIRECT_URI=com.celix.core.desktop://login-callback
